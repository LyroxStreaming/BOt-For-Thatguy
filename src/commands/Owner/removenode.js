const { EmbedBuilder } = require('discord.js');

module.exports = {
    name: 'removenode',
    category: 'Owner',
    description: 'remove voice server from Leco Music™',
    args: false,
    permission: [],
    owner: true,
    execute: async (message, args, client, prefix) => {

    },
};